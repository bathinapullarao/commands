googleapis.dev/ruby/google-api-client/latest  <--api class list
a = varible.split(-)[2]                     <-- variable=ap-dev-32-ide-env  It will give output 32
if $? == 0                                  <-- $? is a global varible in ruby that holds the exit status of the last executed child process
if !varaiblename.empty?                     <-- if not varible value empty
if !variablename.inclue?("Error:_____")     <-- check for that value
if !variblename.nil?
if variablename.match?(/^bathinapu.*rao/)   <--to search for file starts and ends with
v2 = JSON.parse(variablename)                
variable = %x{gcloud command --project=#{gcp_project_id} --format json 2>&1}
puts "discription:   ", varible             <-- It will print the value of the variable
varible = service.http(:get, "https://...")


variable_list.each do |variablename| begin end end
